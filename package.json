{
  "name": "sesori-review",
  "version": "0.2.0",
  "description": "Claude reviews your Figma prototype from inside Figma: flow, screens, questions, Dev Mode annotations.",
  "author": "Sesori",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "private": true,
  "workspaces": [
    "plugin",
    "bridge"
  ],
  "scripts": {
    "build": "npm run build -w plugin",
    "bridge": "npm run start -w bridge",
    "check": "npm run check --workspaces"
  }
}
