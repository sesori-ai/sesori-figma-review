{
  "name": "sesori-review-bridge",
  "version": "0.3.2",
  "description": "Sesori Review — local bridge running the Claude Agent SDK",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/bridge.ts",
    "check": "tsc --noEmit && tsx src/selfcheck.ts && tsx src/provider-activation.check.ts && tsx src/providers/claude.check.ts && tsx src/providers/codex.check.ts && node smoke-lifecycle.check.mjs",
    "smoke": "node smoke.mjs",
    "build": "esbuild src/bridge.ts --bundle --platform=node --format=esm --packages=external --banner:js='#!/usr/bin/env node' --outfile=dist/bridge.mjs --log-level=warning && chmod +x dist/bridge.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.0"
  }
}
